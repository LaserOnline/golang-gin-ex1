{

    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Project GIN",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}",
        }
    ]
}